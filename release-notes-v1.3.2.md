# P-Spider 1.3.2

fig-memo 接入 **Hpoi 手办数据** + 列表增强 + 多项修复。

## fig-memo · Hpoi 联动

- **候选匹配**：详情页右上角悬浮面板，自动给出 Hpoi 候选（**hpoi 网站搜索排序 2 条 + 自研匹配 1 条**），**默认自动关联第一条**，少了逐篇点击。
- **手动校正**：候选不对时点「重新检索」自动排除已显示的取下一批；或「手动校正」直接贴 hpoi 链接 / 词条 id。
- **评分展示与排序**：匹配后展示 Hpoi **评分 / 标签 / 厂商 / 比例 / 发售**；列表支持**按 Hpoi 评分排序**，卡片显示 `★评分`。
- **只对指定分类匹配**：`フィギュアレビュー / レビュー(R18) / 予約開始情報`（可改）。

## fig-memo · 列表与浏览

- **快捷标签墙**：详情页标签改为**点一下即打 / 取消**的 chip 墙，配「＋自定义」；**空格键翻下一篇**。
- **上一篇 / 下一篇**：按当前筛选列表顺序跳转（打标后不跳回列表）。
- **三个小角标**：卡片缩略图右下角显示 已下载（蓝勾）/ 已收藏（红心）/ 已打标签（标签）。
- **图片数修正**：已下载取**本地实际图片数**、未下载取站点图片数，未知不再显示「0 图」。

## 数据内置（随包附带）

- **站点清单**、**文章图片数**、**Hpoi 绑定**数据内置：新用户首次启用**秒开**、离线可用，分类 / 厂商 / 年份标签立即生成。
- **不含个人手标标签**（体型 / 姿势等只保留在本地）。

## 修复

- **fig-memo 加载失败**：XSERVER WAF 拦截 `/wp-json/` 导致「请求失败」反复重试 —— 改用 WordPress `?rest_route=` 形式绕过。
- **比例解析**：`1/6` 曾被解析成分子 `1`，现正确取分母。
- **评分语义**：修正候选回退时误把「评分人数」当评分（如 `★305`）；只采用详情页真实均分。
- **多标签退出**：「多标签」关闭后不再残留筛选。
- **退出全清零**：修正若干退出后残留筛选的情况。

> 绿色版：`P-Spider.exe` 需与 `aria2c.exe` 放在同一目录。
