# P-Spider 1.2.0

## 新功能：本地库

在应用内直接浏览、整理已下载的内容（保存目录下的一级文件夹 = X 作者 / Pawchive 创作者）。

- **标签管理（多标签）**：一个文件夹可同时打多个标签。左侧标签栏含「全部 / 未打标签 / 自定义标签」，支持新建、改名、删除。卡片左上角显示标签数角标。
- **文件夹浏览**：
  - X 型（直接含媒体）→ 文件网格
  - Pawchive 型（含帖子子目录）→ 顶部切换「平铺 / 按文件夹」，支持多级下钻 + 面包屑
- **右键菜单（统一操作入口）**：
  - 文件夹：打开 / 在资源管理器中打开 / 标签（子菜单逐项勾选切换）/ 新建标签并添加 / 属性
  - 文件：打开（系统默认程序）/ 在资源管理器中打开 / 删除文件
- **属性弹窗**：名称、标签、媒体数、全部文件数、占用空间、路径。
- **缩略图缓存**：图片生成小图缓存，二次浏览秒开；设置页可查看当前缓存占用并一键清除（不动原始文件）。
- **排序**：文件夹按 名称 / 日期 / 媒体数；文件按 名称 / 日期 / 类型；均支持升降序。
- **多选批量**：文件批量删除；文件夹批量添加 / 移除 / 清空标签。
- **文件操作**：图片放大预览、视频弹窗播放、定位到资源管理器。

> 首次进入「本地库」会为图片生成缩略图，稍等片刻；之后浏览即可秒开。

## 修复与改进

- 修复含空格路径无法在资源管理器中定位的问题。
- 新增 Rust 命令 `get_path_mtimes`（按日期排序）、`get_folder_stats`（占用空间统计）。
