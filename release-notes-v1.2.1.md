# P-Spider 1.2.1

本地库体验优化 + 若干通用改进。

## 本地库

- **默认排序**：一级文件夹默认「日期 新→旧」；文件夹内默认「名称 Z→A」。
- **日期排序更准**：文件按日期排序改为**按「天」比较**，同一天再用文件名辅助（爬虫批量下载常同天完成，秒级时间差不代表内容先后）。
- **设为文件夹缩略图**：文件右键（图片）可把某张图设为所属作者文件夹的封面；文件夹右键可「恢复默认缩略图」。
- **右键菜单精简**：去掉封面右上角圆形按钮（统一走右键）；「多选」按钮去掉图标。
- **原推文关联**（新增）：
  - 点开图片为 antd 原生全屏预览，右侧固定一条推文信息条：**头像 · 昵称 · 作者 ID · 正文 · 日期**。
  - **点头像 / 作者 ID → 作者主页**；**点日期 → 原推文**；信息条左上角可折叠、右上角关闭。
  - 文件右键新增「打开原网页」。
  - 信息来源：下载历史 → 联网溯源 → 文件名反解（见下）。
- **联网溯源**（新增，设置页「重新溯源本地库（联网）」）：对老文件按作者逐个翻页拉取，用下载模板反推本地路径并匹配，命中即补全推文信息（含正文/头像）。后台运行、可取消、增量落盘。仅 X、需登录、已删除推文无法找回。
- 未联网时，会按**文件名模板反解**出 推文 ID / 用户名 / 时间（默认模板即可），够显示作者/时间/链接。

## 通用

- **窗口自适应**：启动时按当前显示器工作区自适应尺寸并居中，解决高 DPI/多屏下窗口底部被任务栏遮挡。
- **屏蔽原生右键菜单**：不再弹 WebView 自带的「后退/刷新/另存图片」菜单（输入框保留右键粘贴）。
- 修复视频预览被右侧信息条遮挡。
